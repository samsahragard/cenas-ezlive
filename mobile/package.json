{
  "name": "cenas-kitchen-driver",
  "version": "0.1.0",
  "description": "Native Android + iOS shell for the Cenas Kitchen Driver Portal",
  "private": true,
  "scripts": {
    "android:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:release": "cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.21",
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "cenas-gps-native": "file:plugins/cenas-gps-native"
  }
}
