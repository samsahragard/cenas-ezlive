Flask==2.3.3
Flask-Login==0.6.3
Flask-Migrate==4.0.7
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.2
gunicorn==23.0.0

anthropic>=0.40.0

openpyxl>=3.1.0,<4.0.0

pypdfium2>=4.30.0
Pillow>=10.0.0

psycopg==3.3.3
psycopg-binary==3.3.3

pydantic==2.12.5
WTForms==3.2.1
requests==2.32.3
# httpx: explicit (was transitive via anthropic). sam_chat.py's Cena
# gateway path imports it directly to SSE-stream to the aick gateway.
# [socks] extra pulls socksio for SOCKS5 proxy support — needed when
# CENA_PROXY routes outbound through Render's userspace tailscaled.
httpx[socks]>=0.28.1
python-dotenv>=1.0.0
sentry-sdk[flask]>=2.0
# Google Gemini for sam_chat Gemini model option.
google-genai>=1.0.0
